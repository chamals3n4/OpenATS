# Configuration for the Docker Compose stack — holds real credentials.
# Only .env.example belongs in git.
.env
.env.local
.env.*.local

# Local pnpm store (created when pnpm runs with a project-local store path).
.pnpm-store/

.DS_Store
