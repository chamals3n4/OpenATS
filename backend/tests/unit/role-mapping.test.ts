import { describe, it, expect } from "vitest";
import {
  mapToAppRole,
  collectRolesFromPayload,
} from "../../src/shared/auth/verify-token";

describe("mapToAppRole", () => {
  it("map the role names the asgardeo setup script creates", () => {});
});
