node_modules
.next
out

# .env.example is deliberately not re-included: `next build` never reads it,
# and keeping it out of the context stops doc edits from busting the cache.
.env
.env.*

Dockerfile
.dockerignore
README.md

.git
.gitignore
*.log
npm-debug.log*
